" This is vim config file
" 基本结构如图
"vimrc
"subconfig{
"	basic.vim
"	function.vim
"	mapping.vim
"	plugin.vim
"	plugins{
"		...<plugin config>
"	}
"}

" ------------------------------------------------------
"                        基础设置 
" ------------------------------------------------------

" 用于内置基础命令的设置

so $HOME/.vim/subconfig/basic.vim

" ------------------------------------------------------
"                       自定义功能
" ------------------------------------------------------

" 用于存储通过自定义函数实现的功能
so $HOME/.vim/subconfig/function.vim

" ------------------------------------------------------
"                        按键映射 
" ------------------------------------------------------

" 用于存储按键映射mapping设置
so $HOME/.vim/subconfig/mapping.vim

" ------------------------------------------------------
"                        插件相关
" ------------------------------------------------------

" 用于加载插件, 具体插件的配置处于同一文件中, 以<插件名>.vim格式命名
so $HOME/.vim/subconfig/plugin.vim

