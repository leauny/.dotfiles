# Git

> show config file(s) `git config --list --show-origin`
> config mannual `git help config`

## Usage

- use `git diff --staged` to show the difference against the staged files. (--cached / --staged are the same)
- 
